services:
  gatekeeper:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gatekeeper
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - gatekeeper_data:/app/data
      - ../Guardtower:/workspace/Guardtower:ro
      - ../Watchtower:/workspace/Watchtower:ro
      - ../Siegetower:/workspace/Siegetower:ro
    restart: unless-stopped

volumes:
  gatekeeper_data: